<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.hcode.hoj.mapper.ExamHistoryMapper">


    <select id="getIDByUid" resultType="java.lang.Long">
        SELECT id
        from exam_history
        where user_id = #{uid} and type = #{type} and repo_id = #{repoId}
    </select>
    <select id="getRecentID" resultType="java.lang.Long">
        SELECT id
        from exam_history
        where user_id = #{uid} and type = #{type} and repo_id = #{repoId}
        ORDER BY submit_time DESC
            LIMIT 1;
    </select>

</mapper>